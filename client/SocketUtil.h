#ifndef SocketUtil_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define SocketUtil_H

#include <string>

std::string SendSocketMessage(std::string msg);

#endif